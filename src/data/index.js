const { pokemon } = require("./pokemon.json");
const { attacks } = require("./attacks.json");
const { types } = require("./types.json");
const { newPoke } = require("./newPokemon.json");

module.exports = {
  pokemon,
  attacks,
  types,
  newPoke,
};
